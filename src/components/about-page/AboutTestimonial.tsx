import TestimonialSlider from "../common/TestimonialSlider";

const AboutTestimonial = ({ autoplay = false }: { autoplay?: boolean }) => {
  return <TestimonialSlider autoplay={autoplay} titleAlign="center" />;
};

export default AboutTestimonial;
