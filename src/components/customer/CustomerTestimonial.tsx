import TestimonialSlider from "../common/TestimonialSlider";

const CustomerTestimonial = ({ autoplay = false }: { autoplay?: boolean }) => {
  return <TestimonialSlider autoplay={autoplay} titleAlign="left" />;
};

export default CustomerTestimonial;
