interface SlugProps {
  slug: string;
}
const BuildingWrapper = ({ slug }: SlugProps) => {
    console.log("Building slug===>",slug);
    
  return (
    <div>
      
    </div>
  )
}

export default BuildingWrapper
